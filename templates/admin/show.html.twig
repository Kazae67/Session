{% extends 'base.html.twig' %}

{% block title %}Admin{% endblock %}

{% block body %}
    <div class="show-container">
        <h1>Admin</h1>

        <table class="show-table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ admin.id }}</td>
                </tr>
                <tr>
                    <th>LastName</th>
                    <td>{{ admin.lastName }}</td>
                </tr>
                <tr>
                    <th>FirstName</th>
                    <td>{{ admin.firstName }}</td>
                </tr>
                <tr>
                    <th>Email</th>
                    <td>{{ admin.email }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('app_admin_index') }}" class="show-back-button">back to list</a>

        <a href="{{ path('app_admin_edit', {'id': admin.id}) }}" class="show-edit-button">edit</a>

        {{ include('admin/_delete_form.html.twig') }}
    </div>
{% endblock %}
