{% extends 'base.html.twig' %}

{% block title %}New Admin{% endblock %}

{% block body %}
    <div class="container">
        <h1>Create new Admin</h1>

        <div class="form-container">
            {{ include('admin/_form.html.twig') }}
        </div>

        <a href="{{ path('app_admin_index') }}" class="back-button">Back to list</a>
    </div>
{% endblock %}
